---
interface Props {
	date: Date;
}

const date = new Date(Astro.props.date);
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('ja-JP', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
